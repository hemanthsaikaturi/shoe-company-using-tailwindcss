const hamburgerMenu = document.querySelector('#hamburger-menu')
const header = document.querySelectorAll('header')

hamburgerMneu.addEventListener('click', () => {
    hamburgerMneu.classList.add('active')
})